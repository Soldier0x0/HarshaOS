import React from 'react';

const Settings = () => (
  <button
    type="button"
    className="px-3 py-1 text-xs rounded border border-slate-700 hover:border-accent transition"
  >
    Settings
  </button>
);

export default Settings;
